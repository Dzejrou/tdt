#pragma once

namespace ENTITY_STATE
{
	enum VAL
	{
		NONE = 0, NORMAL,
		COUNT
	};
};

enum class FACTION
{
	FRIENDLY = 0, ENEMY, NEUTRAL
};

enum class ENTITY_TYPE
{
	NONE = 0,
	WALL = 1 << 0,
	MINION = 1 << 1,
	ENEMY = 1 << 2,
	BUILDING = 1 << 3,
	MISC = 1 << 4
};

enum class EVENT_TYPE
{
	NONE = 0, KILL_ENTITY, GOLD_DROPPED, RESTORE_SPEED,
	METEOR_FALLING,
	COUNT
};

enum class TASK_TYPE
{
	NONE = 0, GO_TO, GO_NEAR, GO_KILL, KILL, GET_IN_RANGE,
	GO_PICK_UP_GOLD, PICK_UP_GOLD, GO_DEPOSIT_GOLD, DEPOSIT_GOLD,
	COUNT
};

enum class ATTACK_TYPE
{
	NONE = 0, MELEE, RANGED,
	COUNT
};

enum class GAME_STATE
{
	RUNNING = 0, ENDED, PAUSED, MENU, INTRO_MENU
};

enum class TIME_EVENT
{
	NONE = 0, START_EVENT, END_EVENT, CALL_FUNCTION,
	COUNT
};

namespace DIRECTION
{
	enum VAL
	{
		UP = 0, DOWN, LEFT, RIGHT,
		UP_LEFT, UP_RIGHT,
		DOWN_LEFT, DOWN_RIGHT, PORTAL,
		NONE
	};
}

enum class SPELL_TYPE
{
	NONE = 0, TARGETED, POSITIONAL, GLOBAL, PLACING
};

enum class COMMAND_TYPE
{
	MINE = 0, ATTACK, REPOSITION, RETURN_GOLD,
	FALL_BACK,
	COUNT
};

namespace KEY_BIND_ACTION
{
	enum VAL
	{
		NONE = 0, SPELL_BUILD_1, SPELL_BUILD_2, SPELL_BUILD_3, SPELL_BUILD_4,
		NEXT, PREV, SPELL_TAB, BUILD_TAB, MENU_TAB, RESET_CAMERA,
		QUICK_SAVE, QUICK_LOAD,
		COUNT
	};
}

enum class WAVE_STATE
{
	ACTIVE = 0, WAITING, INACTIVE
};

namespace ANIMATION_TYPE
{
	enum VAL
	{
		WALK = 0, IDLE, HIT, ACTIVATE, DEACTIVATE,
		COUNT
	};
}

enum class SELECTION_MARKER_TYPE
{
	CIRCLE = 0, SQUARE, HALF_SQUARE
};